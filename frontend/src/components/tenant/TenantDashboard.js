function TenantDashboard() {
  return (
    <>
      <h2 className="text-center">
        Welcome to Tenant Dashboard
      </h2>
      <div>
        <p>The best way to consume React-Bootstrap is via the npm package<br />
	   whichyou can install with npm (or yarnif you prefer). If you plan<br />
	   on customizing the Bootstrap Sass files, or don't wantto use a CDN<br />
	   for the stylesheet, it may be helpful toinstall vanilla Bootstrapas well.
        </p>
	<p>The best way to consume React-Bootstrap is via the npm package<br />
           whichyou can install with npm (or yarnif you prefer). If you plan<br />
           on customizing the Bootstrap Sass files, or don't wantto use a CDN<br />
           for the stylesheet, it may be helpful toinstall vanilla Bootstrapas well.
        </p>
	<p>The best way to consume React-Bootstrap is via the npm package<br />
           whichyou can install with npm (or yarnif you prefer). If you plan<br />
           on customizing the Bootstrap Sass files, or don't wantto use a CDN<br />
           for the stylesheet, it may be helpful toinstall vanilla Bootstrapas well.
        </p>
	<p>The best way to consume React-Bootstrap is via the npm package<br />
           whichyou can install with npm (or yarnif you prefer). If you plan<br />
           on customizing the Bootstrap Sass files, or don't wantto use a CDN<br />
           for the stylesheet, it may be helpful toinstall vanilla Bootstrapas well.
        </p>
	<p>The best way to consume React-Bootstrap is via the npm package<br />
           whichyou can install with npm (or yarnif you prefer). If you plan<br />
           on customizing the Bootstrap Sass files, or don't wantto use a CDN<br />
           for the stylesheet, it may be helpful toinstall vanilla Bootstrapas well.
        </p>
	<p>The best way to consume React-Bootstrap is via the npm package<br />
           whichyou can install with npm (or yarnif you prefer). If you plan<br />
           on customizing the Bootstrap Sass files, or don't wantto use a CDN<br />
           for the stylesheet, it may be helpful toinstall vanilla Bootstrapas well.
        </p>
	<p>The best way to consume React-Bootstrap is via the npm package<br />
           whichyou can install with npm (or yarnif you prefer). If you plan<br />
           on customizing the Bootstrap Sass files, or don't wantto use a CDN<br />
           for the stylesheet, it may be helpful toinstall vanilla Bootstrapas well.
        </p>
	<p>The best way to consume React-Bootstrap is via the npm package<br />
           whichyou can install with npm (or yarnif you prefer). If you plan<br />
           on customizing the Bootstrap Sass files, or don't wantto use a CDN<br />
           for the stylesheet, it may be helpful toinstall vanilla Bootstrapas well.
        </p>
	<p>The best way to consume React-Bootstrap is via the npm package<br />
           whichyou can install with npm (or yarnif you prefer). If you plan<br />
           on customizing the Bootstrap Sass files, or don't wantto use a CDN<br />
           for the stylesheet, it may be helpful toinstall vanilla Bootstrapas well.
        </p>
	<p>The best way to consume React-Bootstrap is via the npm package<br />
           whichyou can install with npm (or yarnif you prefer). If you plan<br />
           on customizing the Bootstrap Sass files, or don't wantto use a CDN<br />
           for the stylesheet, it may be helpful toinstall vanilla Bootstrapas well.
        </p>
      </div>
    </>
  );
}

export default TenantDashboard;
